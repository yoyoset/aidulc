# Python 侧车
prep/.venv/
prep/build/
prep/dist/
prep/__pycache__/
prep/aidulc_prep/__pycache__/
prep/.pytest_cache/
**/__pycache__/
*.pyc

# Rust
src-tauri/target/
Cargo.lock.bak

# 前端(reader/)
reader/node_modules/

# 打包产物
dist/

# 交付件(二进制不入库; 交接说明见 docs/STAGE_REPORT_2026-08-09.md)
/完整设计交付确认.zip
/生词本和背单词完整设计交付确认.zip
/生词本和背单词完整设计交付确认/

# Phase 0 spike 产物(35000+ 临时 checkpoint + 311MB 模型文件 + 重复 fixtures;
# 有长期价值的 bench_*.json / job_phase3.json 已归档到 docs/measurements/)
.spikes/
*.db
*.db-journal
*.db-wal
*.db-shm

# 系统
.DS_Store
Thumbs.db

# cloud/worker 部署配置(含本机 KV namespace id; 模板 wrangler.toml.template 入库)
cloud/worker/wrangler.toml
cloud/worker/node_modules/
# cloud/mobile 部署状态(上次部署版本标记, deploy_mobile.ps1 维护; 不入库)
cloud/mobile/.last-deployed-version

# wrangler 从仓库根跑会建的状态目录 (P1-D 探测 KV 时实测产生, 非项目文件)
/.wrangler/
/node_modules/
