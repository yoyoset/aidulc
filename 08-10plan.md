# 计划:CF 侧重整 + 自建 VPS 后端(承接 1424 存量)

## Context

问题起点是"CF 这端还需我处理什么"。实测盘点后结论是**服务端基本已就绪**,
但查代码时发现一个测试没覆盖到的硬阻塞,以及由此定下的两条路分工。

**CF 现状(2026-08-10 用只读 wrangler 命令实测)**:
- Worker `aidulc-sync` 已部署(13:24), `ROOT_SECRET` 已设, `/v1/sync`→401、
  `/v1/auth/revoke`→401、未知路由→404, CORS(含 OPTIONS 预检)已处理。
- KV 只有一个 namespace `AIDU_DB`(id `eed0445976d94ba4943cea222b208aaa`), **0 键**。
- Pages `aidulc-mobile` 线上 0.7.0-4。另三个 Pages 项目是无关个人站, 不动。
- **遗留**: V0 冒烟 worker `aidulc-v0-smoke` 仍在公网存活(200), 带无鉴权的 `/kv-test`。

**发现的阻塞**: `cloud/worker/src/index.js` 的推送循环**一词一个 KV 键**
(`srs:{user}:{word}`)。桌面端不分批, 1424 词 = 单请求内约 1424 次 KV 写。
而 `docs/DESIGN_NOTES_SRS.md` 自己抓的免费额度是**写不同键 1,000 次/天** ——
**首次全量同步必然推到一半失败**。该文档当时的推理"配额风险不在一次写几个键,
而在每天几次同步"对首次全量是错的, 其实测样本只到 100 词。

**用户裁决(据此定的两条路)**:
- **CF 免费路径 = 从零积累**:新建空 KV, 词随使用一点点增长, 天然撞不上 1000/天。
  把完整的免费 CF 部署流程整理成跟着用户走的文档(每个自建者用自己的额度)。
- **自建 VPS 路径 = 承接存量**:1424 词正好作为 VPS 路径的压测数据。香港服务器
  **最后只保留"电视中转"与 aidulc 两项, 其余拿掉**;子域名挂 CF 橙云代理拿 TLS。

预期结果:新用户走 CF 免费档不撞墙;你自己这 1424 词落在 VPS 上, 手机能拉到。

---

## S0 设置页卡死主线程(P0, 最先修 —— 现在点设置必假死)

**实测取证(2026-08-10 15:0x, 卡死现场抓的)**:`aidulc.exe` `Responding=False`,
**累计 CPU 仅 0.6 秒**, 40 线程全 `Wait` —— 不是在忙, 是被阻塞。

**根因链**:点设置 → `reader/views/settings_view.js:146` 调 `componentsHealth()` →
`src-tauri/src/commands/misc.rs:37` 是**同步** `#[tauri::command]`(Tauri v2 同步命令跑主线程)
→ `src-tauri/src/services/components.rs:121`:

```rust
let _ = child.stdout.take().map(|mut s| s.read_to_string(&mut out));
```

spawn 6.3GB 的 `aidulc-prep.exe --pymupdf-version`, 然后**无超时**读 stdout 到 EOF。
侧车冷启动慢或管道未关 → 主线程永久阻塞 → 整窗假死, 只能杀进程。

**重要修正**:这与 P0-A(启动自动拉起 stale 侧车)**是两条独立的卡死路径**。P0-A 的修复有效,
但这条一直在, 用户是在修复后的新进程里再次卡死的 —— 之前把"点几个 tab 就卡"整体归因于
P0-A 是不完整的。

**修法(三层, 缺一不可)**:
1. **命令改异步**:`components_health` 改 `async fn`(或内部 `spawn_blocking`), 让它离开主线程。
   **同时排查其余同步命令里有没有同类阻塞调用** —— 这是根子上的问题, 不只这一处。
2. **探测加超时**:`check_pymupdf` 用带超时的读(如 3-5 秒), 超时即返回"探测超时"而非卡住。
   参照 `models_download` 已有的动态超时做法(F4), 不要新造一套。
3. **UI 不等待**:设置页组件健康检查改成异步填充(先出页面, 结果到了再更新), 探测中显示
   "检测中…", 失败显示原因 —— 符合"后台失败必须可见"。

**验收**:① 侧车被人为改名/删除时点设置, 页面 3-5 秒内出结果且窗口全程 Responding;
② 侧车正常时点设置, 窗口不假死;③ 加一条不依赖真实侧车的单测(注入一个永不输出的假进程,
断言在超时内返回)。

## S1 CF 侧重整(低风险, 先做)

1. **新建空 KV namespace**(`vi` 只是代号, 用能看出用途的名字 `aidulc-sync-kv`):
   `wrangler kv namespace create aidulc-sync-kv` → 把返回 id 填进
   `cloud/worker/wrangler.toml`(本地文件, 已 gitignore)的 `binding = "DB"`。
2. **删除遗留 `aidulc-v0-smoke`**:`wrangler delete --name aidulc-v0-smoke`。
   它带无鉴权的 `/kv-test` 端点裸露在公网。**执行前我会再问一次**(删除不可逆)。
3. **旧 `AIDU_DB` 的处置**:P1-D 已证明它是空的(0 键)、生词其实在 Chrome 扩展里,
   所以删掉无数据损失。**但它是旧 AIDU 的遗产, 默认保留不动**, 你说删我再删。
4. **文档对齐现实**:`cloud/worker/README.md` 写的是创建 `AIDULC_SYNC`, 实际复用了
   `AIDU_DB` —— 照文档做的人会踩空。改成与真实步骤一致。
5. **产出《自建 CF 同步服务》一份**(你要的"跟随用户走"的完整流程):从 `wrangler login`
   → 建 namespace → 填 wrangler.toml → `secret put ROOT_SECRET` → `deploy` → 桌面端连接,
   每步带预期输出。放 `cloud/worker/README.md` 或独立 `docs/SELFHOST_CF.md`。

## S2 写配额护栏(两条路都要, 属"后台失败不许静默")

现在推一半撞配额的行为**没有测过**, 会留下部分同步的混乱状态。

- **桌面端推送前估算写次数**:待推词条数 + 1(deck index) 若超过阈值, **明确拒绝并告知**
  "本次需 N 次写, 免费档每天 1000 —— 请分批或改用自建后端", 而不是发出去撞。
- **worker 侧**:捕获 KV 写失败(配额/限流), 返回明确错误码与"已写入 M 条"的部分结果;
  客户端据此更新 `last_push_at` 到真实进度, **不能把失败当成功**。
- 配回归测试:模拟写失败, 断言不会把未推成功的词标记成已推。

## S3 自建 VPS 后端(承接 1424 存量)

**代码侧缺的只有一层壳** —— `src/storage.js` 的 `createFileKv` 已就位, `index.js` 在
无 `env.DB` 时自动用它, CORS 也已处理。缺的是 Node 的 HTTP 入口(现在只有 CF Worker 的
`fetch` 导出, `local_test.mjs` 是直接调函数不走网络)。

1. **新增 `cloud/worker/src/server.mjs`**:`node:http` → 适配成 `Request` 交给既有
   `fetch(request, env)`, `env = { KV_DIR, ROOT_SECRET }`。**业务代码一行不改**,
   同一份 `index.js` 两端跑 —— 这正是 V5 做存储抽象的目的。
2. **香港服务器改造(顺序不可颠倒)**:
   a. **先盘点再动手**:列出 systemd 服务 / docker 容器 / 端口占用 / 定时任务,
      **产出清单交你确认哪些拿掉**。电视中转保留。**不做任何未经你逐项确认的停用或删除。**
   b. 装 Node → 部署 `cloud/worker` → `systemd` 守护 → 监听本机 HTTP 端口。
   c. 子域名 A 记录指向香港机 + **开橙云代理**(CF 自动 TLS, 服务器只跑 http)。
      注意端口要用 CF 代理支持的(经 nginx/caddy 落 80, 或直接 8080)。
   d. `KV_DIR` 数据目录纳入备份(它就是你的词库真相源之一)。
3. **访问方式**:我没有那台机器的凭据。要么你给我 SSH, 要么我把命令逐条写好你执行。
   这一步在计划批准后你选。

## S4 顶栏用户下拉完善(现在是个死控件)

`reader/views/shell_view.js:80-112`。实测(截图)下拉能正常展开, **之前那次窗口卡死与它无关**
(是启动自动拉起侧车, P0-A 已修), 但它本身有三个问题:

1. **只有一个用户时点开只有"我"一项, 什么也做不了** —— 用户第一反应是"这是什么"。
   加 **"＋ 新建成员"** 项:选中 → 弹输入名字 → `users_repo` 新建 → 切过去。
2. **选项文字里硬拼了 `" ▾"`**(第 88 行 `(u.name || u.id) + ' ▾'`)。原生 `<select>` 自带箭头,
   结果展开的列表项里显示成"我 ▾"。去掉, 箭头交给控件本身。
3. **`change` 语义要兜住取消**:选了"＋ 新建成员"又取消输入时, 必须把 `select.value`
   回滚到当前 user, 否则下拉停在一个不存在的选项上。

**必须说清的一致性问题(别做错)**:本地"新建成员"与服务端 `invite-user` 是两件事。
token 是**按本地 user 分账存**的(`credentials.rs::save_cf_token_for(user_id, ...)`),
所以新建的本地成员**天然没有 token → 同步状态显示"同步未连接"是正确行为**,
需要单独用 6 位 `invite-user` 码配对后才会同步。
新建成员时**不要**去复用当前 user 的 token —— 那会把两个人的词推进同一个服务端 user。
UI 上要让这件事可见:新建完成后提示"这位成员还没连同步, 去设置页邀请"。

顺带:服务端 user_id 与本地 user_id 可能不同(服务端从 token 解析), 现在没有地方记录二者对应。
建议配对成功时把服务端返回的 user_id 一并存下来, 只为排查问题用, 不参与业务逻辑。

验收:`_smoke_views.mjs` 加一节 —— 单用户时下拉含"＋ 新建成员";新建后顶栏出现两项、
当前项切到新成员、其同步状态为"同步未连接";取消新建时 value 回滚。

## S5 句前播放按钮:改成"只播这一句"(跟读现在没法用)

**现状**(`reader/views/reader/player.js:156` `playFrom`):整章是**一条音频流**, 句前按钮只是把
`currentTime` 定位到该句起点然后 `play()`;`_tick()`(201 行)里**没有任何"播到本句末尾就停"
的判断** → 点一句就一路播到章末, 跟读无从谈起。跟读的重复逻辑
(`core/shadow.js` 的 `shouldLoopBack`)存在, 但**句前按钮没接它**。

**规格(两种都要, 用已有的 `pace` 概念切换)**:
- **逐句模式 = 单句停**:播到该句 `audio.end_ms` 即暂停, 锚点停在该句。
  若当前是跟读类预设(repeat N), 按预设重复 N 次、每次留白后再停 —— 复用 `shadow`, 不另写。
- **通篇模式 = 连续播**:维持现状, 一路往下。
- 沿用现成的 `pace`(通篇/逐句, 已有 `reader-content[data-pace]` 与 `core/reader_state.js`),
  **不新增设置项**;句前按钮的图标/提示要能看出当前是哪种语义。
- 实现:`player` 加 `playOne(index)` 设 `_stopAtMs = s.audio.end_ms`, `_tick` 里越过即
  `pause()` 并清除;与 `shouldLoopBack` 的优先级要理清(重复未用完不停)。

**顺带记一笔(不在本次范围)**:`ended` 监听(138 行)用
`Math.abs(currentTime*1000 - end_ms) < 200` 反查句索引, 200ms 窗口很脆, 且整章流只在章末
触发一次。改单句停止后这条路径的作用要重新评估。

**验收**:`reader/core/*.js` 纯逻辑单测 —— 给定句区间与 stopAt, 越界即产出 pause 指令;
DOM 冒烟:点句前按钮 → 播放 → 到句末自动暂停且锚点不前移;跟读预设下重复 N 次后才停。

## S6 阅读器语义通道:重新定义颜色区隔(现在"跳动"看不见)

**实测问题**:
- `--rd-reading-bg` `#f0e7da` vs 纸面 `--rd-surface` `#fbf8f3` —— 两个都是暖白, **逐词跳动几乎不可见**。
- **色系覆盖不全**:5 个 palette 只覆盖 `--rd-accent`;`--rd-reading-bg` / `--rd-mark`
  只在 `:root` 与 dark 定义 → 换色系时朗读底色、短语线纹丝不动。
- **通道互相撞色**:生词下划线用 `--rd-ink-3`(= 讲解文字的灰, 不成为独立通道);
  短语线 `--rd-mark` `#a8792e` 与 `--rd-accent` `#9c6a4a` 色相接近;摘录用
  `--md-sys-color-warning` 又与 `--rd-mark` 撞。
- **门禁盲区**:`scripts/check_contrast.mjs` 只查 ink/ink-2/ink-3 对 surface,
  朗读底色/生词线/短语线/摘录**全不在门禁内** —— 所以"看不见"没有任何自动防线。

**设计稿是权威, 不要重新发明**(`完整设计交付确认/aidulc 完整设计.dc.html`):
- §06 原话:**"阅读界面里唯一带彩度的东西是当前朗读词底色和当前句标线"** ——
  实现给的 `#f0e7da` 几乎没有彩度, **直接违背这句**。用户选定"明显可见优先"与设计稿一致,
  不是偏离设计语言, 而是**回到设计语言**。
- §06 四通道结构(朗读 = `--rd-reading-bg` 底色 / 已在生词本 = 1px 下划线 /
  短语动词 = 2px `--rd-mark` 下划线 / 可查词 = hover 底, **四者可叠加而不互相遮蔽**)
  —— 结构实现是对的, **错在取值**。保结构、改值。
- §08 "四色系:中性色不变, **只换强调色家族**" —— 朗读底色属于强调色家族,
  **本就该跟着色系变**, 现在不变是漏做。

**做法**:
1. **通道清单**(在设计稿四通道基础上补全实际存在的):当前句 / 正在朗读的词 / 生词 /
   短语动词 / 摘录 / 文本选中。
2. **双编码**:形态 + 颜色, 不靠单一颜色区分(色盲友好);形态分配沿用设计稿, 不改。
3. **每个通道在 5 色系 × 明暗都给值**(共 10 组), 写进 `tokens.css`, 不再留"只有 root 有"的通道。
4. **门禁扩展**(关键, 否则会再退化):`check_contrast.mjs` 增加
   ① 朗读底色 vs 纸面的**可察觉亮度差**下限;② 各通道颜色两两之间的可判别差;
   ③ 正文墨色压在每个通道底色上仍过 WCAG AA。阈值定下来后写进脚本, 数值只能提高不能放宽。

**验收**:门禁新增项全绿;5 个色系 × 明暗逐一截图对照, 朗读跳动在每种组合下都肉眼可辨;
生词/短语/摘录三者在同一句里同时出现时仍能分清(构造一个三者共存的冒烟用例)。

## S6b 设计语言符合性审计(用户要求"还有哪些没遵循, 一并改")

S6 暴露的模式是:**结构照做了, 取值做丢了, 而且没有门禁兜底**。所以不能只修朗读底色这一处,
要把设计稿里**可机械核对的规则**整体过一遍。

**方法**(不要凭印象改, 也不要一次性大重构):
1. 把 `完整设计交付确认/aidulc 完整设计.dc.html` 里的硬规则抽成一张清单
   (每条:设计稿原话 → 可核对的判据 → 当前实现 → 符合/偏离)。产出
   `docs/DESIGN_CONFORMANCE.md`。
2. **偏离项分三类**处理:① 明显 bug 级(如朗读底色)→ 直接修;② 有意偏离 → 写清理由留档;
   ③ 需要大改 → 记 ROADMAP, 不在本轮做。
3. **能变门禁的一律变门禁** —— 这是防止再退化的唯一办法, 和 `css:no-raw-hex` / `no-blk-texture`
   同一思路。

**已扫到的线索(供起步, 不是完整清单)**:
- 虚线规则"1.5px dashed + 40% 纸面填充, 全系统唯一, 只在空态":`library.css:81` 与
  `prep.css:3` 都是 1.5px dashed, **看起来合规**, 但要确认填充与使用场合。
- "逐帧只允许 `background-color` 与节奏线的 `translate3d + scaleX`"、"阅读器内禁用
  `backdrop-filter`"、"深色不加阴影"、"一屏一色:唯一的彩色是「开始处理」"、
  "Toast 三种(固定深底白字, 底部居中, 2.5s)"、"设置用纵列(横向 tab 超 5 项不可扫读)"
  —— 这几条都可机械核对, 逐条查。
- 已有门禁只覆盖 `css:no-raw-hex` / `.atomic-block` 禁滤镜纹理 / 对比度四对,
  **上述规则一条都没进门禁**。

**验收**:`DESIGN_CONFORMANCE.md` 每条都有判据与结论(不允许"大致符合"这种措辞);
新增门禁项在 `check.ps1` 里全绿;修掉的偏离项各自有提交与真实前后对照。

## S7 端到端验证(机械可核对)

1. **VPS 路径**:桌面端 worker 地址指向新子域名 → 连接 → 推 1424 词 →
   `KV_DIR` 里键数 = 1425(1424 词 + deck index) → 手机扫码 → 今日队列 1291 到期
   (= 当前 review 阶段词数, 已掌握 133 不进队列)。
2. **CF 免费路径**:用新建的空 namespace 起一个实例, 从 0 词开始加 20 词 → 同步成功,
   全程写次数 ≤ 25, 证明"从零积累"不撞配额。
3. **护栏**:构造 1424 词推向 CF 实例, 断言**被前置拒绝**并给出人话提示, 而不是推一半。
4. **顶栏下拉**:单用户时含"＋ 新建成员";新建"儿子"后两项都在、切过去后生词本为空、
   同步状态为"同步未连接"(证明没有复用当前 user 的 token)。
5. `scripts\check.ps1` 全绿(含新增的 server.mjs 与下拉相关测试)。

## 纪律

一条一提交;落地前 `check.ps1` 全绿;clippy 基线只降不升;新 `.ps1` UTF-8 BOM;
真实数字写进提交信息;收尾按 `BOOK_WORKFLOW.md §6` 重打便携版。
**涉及你线上资源的删除(worker / 服务器上的服务)一律先出清单、你点头再执行。**
